<form method="post" action="{{ path('crud_carousel_delete', {'id': carousel.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ carousel.id) }}">
	<button class="btn">Supprimer</button>
</form>
