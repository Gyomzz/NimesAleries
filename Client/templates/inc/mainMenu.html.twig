<div
	class="menu-area">
	<!-- Main Menu -->
	<nav class="navbar navbar-expand-lg">
		<div class="navbar-collapse">
			<div class="nav-inner">
				<ul class="nav main-menu menu navbar-nav">
					<li class="{{activeMenu([''])}}">
						<a href="{{path('index')}}">Accueil</a>
					</li>
					<li class="{{activeMenu(['produits'])}}">
						<a href="{{path('produits')}}">Produits</a>
					</li>
					<li>
						<a href="#">Services</a>
					</li>
					<li>
						<a href="#">Pages</a>
					</li>
					<li class="{{activeMenu(['contact'])}}">
						<a href="{{path('contact')}}">Contact</a>
					</li>
					<li class="{{activeMenu(['cart'])}}">
						<a href="{{path('cart')}}">Panier</a>
					</li>
				</ul>
			</div>
		</div>
	</nav>
	<!--/ End Main Menu -->
</div>
